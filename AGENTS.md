# 项目协作约定

## 项目目标

这是一个无需后端、无需构建、可直接打开 `index.html` 游玩的经典三人单机斗地主。优先级依次为规则正确、流程完整、操作清楚和界面品质。

## 工程约束

- 正式代码使用原生 HTML、CSS、JavaScript，不新增运行时依赖或远程脚本。
- 浏览器代码采用按顺序加载的经典脚本，并统一挂到 `globalThis.DDZ`；规则文件同时兼容 CommonJS，供 Node 测试加载。
- 规则、状态、AI、渲染和 DOM 事件保持分层；DOM 事件不得自行修改牌局字段。
- 扑克牌以唯一 `id` 和数值 `rank` 表示，所有删牌与归属校验都按 `id` 完成。
- 非法动作必须保持牌局状态完全不变，并返回明确中文错误。
- AI 异步回合必须校验 `roundId` / `revision`，避免重新开始后旧计时器继续落子。
- 不使用来历不明的图片、字体、音乐或音效；关键视觉由 HTML/CSS/Unicode 生成，声音由 Web Audio API 合成。

## 规则口径

- 顺子、连对、飞机主体最高只能到 A，不能包含 2 或王。
- 飞机带单采用严格口径：翅膀点数互不相同，且不与主体重合。
- 四带二单允许附件恰好是一对；四带二对要求两个不同对子。
- 普通牌型必须同型同长度才能比较；炸弹压普通牌，王炸最大。

## 验证要求

- 修改规则或 AI 后运行 `npm test`（或直接使用 Node 执行 `node --test ...`）。
- 修改界面后至少验证主菜单、叫分、出牌、提示、不出、两家不出重置、胜负、重开和返回菜单。
- 浏览器验收需检查桌面与窄屏视口、键盘操作、控制台错误、外部网络请求和刷新回到安全主菜单。

## 源码加载顺序

1. `src/game/cards.js`、`deck.js`
2. `hand-analyzer.js`、`hand-comparator.js`、`rules.js`
3. `src/ai/*`
4. `game-state.js`
5. `src/ui/*`
6. `game-controller.js`、`main.js`
